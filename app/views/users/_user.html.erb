     
      <% if user.image.present? %>
		<p><%= image_tag user.image_url(:thumb).to_s %></p>
	  <% else %>
		<p><%= image_tag ('default_user_profile_small.jpg') %></p>
	  <% end %>
	  
  <strong><%= link_to user.name, user %></strong>
  <% if !current_user?(user) %>
  <%= button_to "Add as Friend", friendships_path(:friend_id => user), :method => :post %>
  <% end %>
  	
 <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "R You sure?" } %>
  <% end %>
</li>

   

